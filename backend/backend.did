service : {
  addHighScore: (text, int) -> ();
  getHighScores: () -> (vec record {
                              text;
                              int;
                            }) query;
  getProgress: (text) -> (opt int) query;
  saveProgress: (text, int) -> ();
}
